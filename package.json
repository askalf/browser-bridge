{
  "name": "browser-bridge-image",
  "private": true,
  "version": "0.5.1",
  "description": "Build-time package manifest for the browser-bridge Docker image. Not published to npm — install only resolves puppeteer-extra deps inside the container.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "fuzz": "node fuzz/run.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "puppeteer-core": "^25.5.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "puppeteer-extra-plugin-user-data-dir": "2.3.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@jazzer.js/core": "^4.0.0"
  },
  "overrides": {
    "@hono/node-server": "^2.0.5",
    "fast-uri": "^3.1.5",
    "puppeteer-extra-plugin-user-data-dir": "2.3.3",
    "tar": "^7.5.21"
  }
}
